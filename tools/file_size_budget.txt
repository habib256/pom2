# POM2 file-size budget — see tools/check_file_sizes.sh
#
# Ceilings for every first-party file at or above 2000 lines.
# A number here may go DOWN (record the win); it may not go up.
# Growing one of these means editing this file in the same commit.
#
# 2026-08-28: src/MainWindow.cpp LEFT THIS FILE. It was 8316 lines and had
# been the largest entry since the ratchet was written; the decomposition
# (TODO P1-3) took it to ~1680, below the 2000-line watch threshold, and every
# TU it was split into is below it too. The two exceptions this header used to
# record — the GPL-notice bump and the host-seam injections — went with it.
#
# The MainWindow family also has a HARD cap now, enforced at configure time
# rather than here: pom2_enforce_mainwindow_line_limit() fails cmake if any
# src/MainWindow*.cpp passes 2000 lines. The ratchet is the general rule; that
# is the specific promise, because this is the family that broke it before.
#
# 2026-08-31: TWO CEILINGS RAISED, deliberately, and the split is still owed.
# The gate had been red on main since 2026-08-29 — failing in 15 s, before the
# Linux job compiled anything, so it was also hiding the build and the GLES
# tier behind it. Raising the two numbers is what this file is FOR (see the
# script header: editing it is the moment someone gets asked), but it is the
# lesser half of the answer and the debt is recorded in TODO § [Arch]:
#
#   src/ImageWriter.cpp  2152 -> 2501  (+349) — the printer work: the
#       ImageWriter head, the paper tray, PDF export and the PostScript /
#       screen-dump seams all landed in one TU. It has clean internal seams
#       and wants splitting, not a bigger number.
#   src/Apple2Display.cpp                    2382 -> 2713 (+2) — the irreducible cost of the
#     Videx LOWER CASE CHIP: two glyph helpers each take one more parameter
#     (whether the character ROM has lowercase). The 24 lines that came with
#     it went into src/CharRomDump.cpp instead, which is also what let
#     Memory.cpp go DOWN 2442 -> 2429 in the same change.
#   src/Memory.cpp       2402 -> 2442  (+40) — smaller and not one change:
#       +1 of it is the foreign-bus dispatch that lets a coprocessor card run
#       the 6502 over its own map (PERFORMANCE § 9); the rest predates it.
#
# Both are recorded at their EXACT current size, so the ratchet still fails on
# the next line either of them gains.
#
# 2026-09-01: src/Apple2Display.cpp 2294 -> 2486. The Le Chat Mauve painters
# moved to their own TU (src/Apple2Display_ChatMauve.cpp) with the shared
# page/band helpers in src/Apple2Display_Internal.h — the first time this
# file's ceiling went DOWN.
#
# 2026-09-02: src/Memory.cpp 2429 -> 2442. The dual-bank character ROM
# (annunciator-2 font switch, Block ASCII Anthology): loadCharRom keeps both
# 4 KB banks and charRomActiveData()/charRomActiveSize() pick the live set
# from an2. Small and localised; recorded at its exact size so the ratchet
# still fails on the next line it gains.
#
# 2026-09-07: SIX CEILINGS RAISED, deliberately, by the two bug-hunt rounds of
# 2026-09-06/07 (commits 266a289..70d86a2). Every line is a fix or the comment
# that explains it, in the file where the defect lived; none is a new feature:
#
#   src/Apple2Display.cpp        2294 -> 2382 (+88)  published-frame state on
#       every path, the idle-switch override for a stopped machine, AppleWin Tv
#       first-frame flags.
#   src/DiskImage.cpp            2835 -> 2914 (+79)  WOZ saveDirty refuses an
#       unsplicable track instead of dropping it; prepareTempPath + the unique
#       temp name; writeFraming in the media capture; the media-write epoch.
#   src/ImageWriter.cpp          2501 -> 2531 (+30)  paced-drain work budget,
#       Diablo ESC P unit, the 16-entry density table, pageToRgba bound.
#   src/M6502.cpp                2159 -> 2185 (+26)  the interrupt split when a
#       debug hook is attached, so a breakpoint on an IRQ handler entry fires.
#   src/Memory.cpp               2442 -> 2545 (+103) AN0-2, vblWasActive and
#       the //c $C800 latch in the snapshot trailer; the No-Slot Clock and the
#       Sony 3.5" mechanisms as trailer sections; the $C000-$C07F write-side
#       window close.
#   src/hgrpaint/HgrPaintEditor.cpp          2525 -> 2647 (+122) nesting-exact stroke
#       brackets (the Ctrl+X-mid-drag lockup), Copy/Cut flush, undo cleared on
#       Load, overwrite confirmation, the screen-hole tag inside undo.
#
# All six are recorded at their EXACT current size, so the ratchet still fails
# on the next line any of them gains. The splits owed remain owed (TODO § Arch):
# Memory.cpp's snapshot trailer and DiskImage.cpp's WOZ writer are the two
# obvious seams.
#
# 2026-09-07 (bug hunt #3): FIVE CEILINGS RAISED AGAIN and one file joined the
# list. Same shape as the morning entry — every line is a fix or its rationale:
#
#   src/Apple2Display.cpp        2382 -> 2395 (+13)  idle-override gate on the
#       published frame index, not the cycle counter.
#   src/DiskImage.cpp            2914 -> 2994 (+80)  weak-bit noise on long
#       flux gaps (MAME amplifier-freakout), the LSS-cell arithmetic note.
#       SIX LINES FROM THE 3000 HARD CAP: the next change to this file must
#       be a split (the WOZ writer and the flux view are the seams).
#   src/M6502.cpp                2185 -> 2204 (+19)  one epilogue in step()
#       plus the POM2_UNLIKELY macro that keeps debugHook_ off the hot path.
#   src/Memory.cpp               2545 -> 2570 (+25)  RDIOUDIS //c-only with
#       the floating bus, $C060 bus bits, the slot-bus floating source.
#   src/hgrpaint/HgrPaintEditor.cpp 2647 -> 2653 (+6) nesting-exact endBatch,
#       flushOpenStroke gated on the bracket.
#   src/StorageCoordinator.cpp   (new) 2018   crossed the watch line with the
#       ejectAllMedia three-phase walk and the disk35 key persistence;
#       recorded at its exact size.
#
# 2026-09-07: FIVE CEILINGS RAISED for bug hunt #4, and ONE SPLIT taken.
#
# The split first, because it is the one this file exists to force. The new
# side-effect-free paged CPU view (`peekCpuView` / `snapshotCpuView` /
# `peekCpuWriteTarget` — what the 6502 would fetch, for inspectors that must
# not clock the Disk II or clear the keyboard strobe) went into its own
# translation unit, **src/MemoryCpuView.cpp**. It is a self-contained question
# about the paging model, three functions and their reasoning, and it was the
# only one of the five whose growth was mostly CODE. Memory.cpp still ends the
# round +10 rather than +89.
#
# The other four are dominated by the "why" comments this project's own
# conventions require at every fix site — measured over the round's diff:
# ImageWriter 141 of 196 added lines, M6502 45 of 58, Apple2Display 18 of 33,
# StorageCoordinator 51 of 110. All five stay well under the 3000-line target.
#
#   src/ImageWriter.cpp          2531 -> 2686 (+155) the ImageWriter II read
#       against its actual Technical Reference for the first time: US n, the
#       tab/margin numbering split, perforation skip on switch B-3, the
#       proportional dot unit, ESC v, and the Epson margins that were not
#       being consumed. Each carries the manual's own wording, because the
#       next reader will otherwise "fix" it back to the port's behaviour.
#   src/StorageCoordinator.cpp   2018 -> 2130 (+112) setMediaBayType rebuilt
#       into the three-phase capture/commit/re-resolve shape every other
#       eject already had, plus the rewind invalidation applySmartPortPanel
#       was missing on all three of its media paths.
#   src/Memory.cpp               2570 -> 2580 (+10)  what is LEFT after the
#       split above: the RamWorks bank count folded into the snapshot
#       identity, and its out-parameter.
#   src/Apple2Display.cpp        2395 -> 2418 (+23)  phosphor decay paced by
#       elapsed EMULATED frames in the three painters that still scaled it
#       per render call, and the idle-override clamp beside it.
#   src/M6502.cpp                2204 -> 2217 (+13)  the interrupt entry's
#       seven cycles published before the handler's first instruction, and
#       the derived IRQ flag deleted in favour of reading the source mask.
#
# Regenerate with: tools/check_file_sizes.sh --update
#
# 2026-09-08: the ProDOS rounds and bug hunt #5, six Opus hunters. Every
# hunk below is a confirmed defect with its pin, and most of each delta is
# the comment that says WHY (the repo's rule); none of it is new feature
# code, and none of it is the split R4 still owes.
#   src/Apple2Display.cpp        2418 -> 2432 (+14)  VBL events belong to the
#     next frame; the mixed text band is kept only when the frame ENDS mixed;
#     force560_ pins a straddling 80/40-col beam frame to one buffer.
#   src/DiskImage.cpp            2994 -> 3078 (+84)  the DOS 3.3 half of the
#     sector-order sniff (VTOC + catalog chain), the address-field checksum
#     gate (x2, 16 and 13 sectors), WOZ shared-TRK alias mirroring.
#   src/ImageWriter.cpp          2686 -> 2706 (+20)  ESC V/U restore switch
#     B-6's mask; Epson ESC R keeps ESC N's margins.
#   src/M6502.cpp                2217 -> 2273 (+56)  eight undocumented-NMOS
#     cycle stubs and the per-column table mapping (Tom Harte totals).
#   src/Memory.cpp               2580 -> 2594 (+14)  the No-Slot Clock under a
#     slot card's ROM (read + write hooks).
#   src/StorageCoordinator.cpp   2130 -> 2167 (+37)  re-synthesise a host
#     folder after flushing the same folder (the second save no longer
#     reverts the first).
# DiskImage.cpp crossed 3000. R4's debt grows by these 225 lines and is not
# forgiven: the DOS/ProDOS sniff pair and the WOZ writer are the two seams a
# split of DiskImage.cpp would cut along.
#
# 2026-09-08 (later): bug hunt #6, six more Opus hunters, and the four
# leftovers of #5. Same shape as the block above: confirmed defects with
# their why-comments, no feature code, no split.
#   src/Apple2Display.cpp        2432 -> 2462 (+30)  patchMixedTextBand walks
#     the beam segments inside rows 160-191 (the composite text band).
#   src/ImageWriter.cpp          2706 -> 2720 (+14)  the DMP eats ESC g's body.
#   src/Memory.cpp               2594 -> 2604 (+10)  accessCycle() (the clock
#     the video-event log stamps with) and the $C000 paste hand-over.

# 2026-09-08 (later still): bug hunt #7, the Disk II half. Two why-comments
# and the `resumes` branch in writeFlux; no feature code, no split.
#   src/DiskImage.cpp            3078 -> 3096 (+18)  a burst re-armed within 8
#     nibble-times carries its cursor forward (DOS INIT's Q7 drop between
#     the address and data fields).
# 2026-09-08 (bug hunt #8): two ceilings raised for the CPU/MMU lot. A
# why-comment and one 8-line function each; no feature code, no split.
#   src/M6502.cpp                2273 -> 2300 (+27)  nmosIndexDummyRead(): the
#     NMOS un-fixed-address read on indexed stores and page-crossing loads.
#   src/Memory.cpp               2604 -> 2616 (+12)  INTC8ROM latches on a $C3xx
#     WRITE under INTCXROM too (the write path returned before the case).
# 2026-09-08 (media write by default): one ceiling raised for the policy
# change — the absent-key default of every `*_writeback` reader now comes
# from MediaWritePolicy.h; no feature code, no split.
#   src/StorageCoordinator.cpp   2167 -> 2169 (+2)  the Disk II default reads
#     the policy for the primary and the non-primary card alike.
# 2026-09-08 (bug hunt #10): two ceilings raised. Why-comments and the
# smallest code that fixes each defect; no feature code, no split.
#   src/Apple2Display.cpp        2462 -> 2491 (+29)  textLitColor() (text wears
#     the phosphor), the zero-decay rule, the lowercase fold removed.
#   src/ImageWriter.cpp          2720 -> 2764 (+44)  ESC p consumed, ESC ! masks
#     italic/proportional, ESC ( keeps its chain past 32 stops, A-8 feeds once.
# 2026-09-08 (bug hunt #11): one ceiling raised for the lock rule.
#   src/StorageCoordinator.cpp   2169 -> 2290 (+121)  flushOutgoingBay(): the
#     outgoing medium's write-back moves off stateMutex before a mount
#     replaces it (three call sites), and the SmartPort panel's ejects run
#     through ejectMediaBay after the critical section. A split of the
#     bay-flush helpers into their own TU is owed (TODO § Arch).
# 2026-09-08 (the notch): two ceilings raised. The notch itself lives in its
# own TU (StorageCoordinator_Notch.cpp: setMediaNotch + the legacy-key
# migration); what remains in the god-objects is the smallest wiring.
#   src/StorageCoordinator.cpp   2290 -> 2322 (+32)  restoreMediaFromSettings
#     migrates a legacy `*_writeback = false` key into the notch at its five
#     restore sites, and re-probes the host bit on each restored medium.
#   src/DiskImage.cpp            3096 -> 3100 (+4)   `hostReadOnly_` reset in
#     the four buffer loaders (a from-buffer image has no host file).
# 2026-09-08 (eight SmartPort units): one ceiling raised for the wiring —
# the panel's unit-count command applied and persisted, the count in the
# panel snapshot, the restore of `smartport_slotN_units`; no feature code.
#   src/StorageCoordinator.cpp   2322 -> 2337 (+15)
# 2026-09-09 (bug hunt #12): one ceiling raised for a one-line guard and
# its comment — `writeNibbleAt` refuses a WOZ, whose surface is the bit
# stream (a nibble write blanked the quarter-track). The split owed on
# DiskImage.cpp (the WOZ writer) is not forgiven by these nine lines.
#   src/DiskImage.cpp            3100 -> 3109 (+9)
#   src/Apple2Display.cpp        2491 -> 2500 (+9)   the signal builder records
#     that it blanked the mixed text band (`mixedBandLeftBlack_`), and the
#     three patch gates read that instead of the frame's final state; the
#     dead whole-frame `mixedGfx` walk went away with it.
# 2026-09-09 (bug hunt #13): two ceilings raised for the snapshot trailer
# and one helper split — the $C800 expansion-window OWNER joins Memory's
# paging/IOU flip-flop section (a tenth byte, with the why next to it), and
# the language card's RAM decode is one function for the read path and the
# debugger's write target. The seam the debt names, Memory.cpp's snapshot
# trailer, is still the split owed.
#   src/Memory.cpp               2616 -> 2644 (+28)
# 2026-09-09 (bug hunt #14): one ceiling raised for the undocumented READ
# opcodes' bus access — two addressing-mode placeholders split from their
# store-form siblings (UnoffAbsSt, UnoffInd6R) and the why next to them. The
# opcode-table shape and the dispatch are untouched.
#   src/M6502.cpp                2300 -> 2346 (+46)
# 2026-09-09 (bug hunt #15): one ceiling raised for three warnings — the
# Disk II, HDV and CFFA restore branches now say "persisted path not found"
# like the SmartPort and generic-bay loops always did, with the why once.
#   src/StorageCoordinator.cpp   2337 -> 2357 (+20)
#   src/Apple2Display.cpp        2500 -> 2522 (+22)  the DLGR bit stream is
#     MAME's word builder verbatim (raw aux nibble, main one sample later)
#     and the is_80_column phase term, with the why at the three sites.
#   src/ImageWriter.cpp          2764 -> 2798 (+34)  the paper-tray rules said
#     once at each of the three sites: Clear all wipes the platen before the
#     power cycle, and a data-stream form feed ejects on every head.
# 2026-09-09 (bug hunt #16): one ceiling raised for the weak-zone model —
# MAME's 4 us / 50 % pulse train across the whole erased stretch, hashed per
# revolution, replacing the single blip per revolution — with the why next
# to it. Still the same seam owed: DiskImage.cpp's flux/WOZ half is a TU of
# its own waiting to happen.
#   src/DiskImage.cpp            3109 -> 3154 (+45)
# 2026-09-09 (bug hunt #17): one ceiling raised for two //c status reads
# MAME's c000_iic_r answers and POM2 dropped on the floating bus — RDVBLMSK
# ($C041) and RDDHIRES ($C079-$C07F odd) — with the why next to them.
#   src/Memory.cpp               2644 -> 2682 (+38)
#   src/Memory.cpp               2682 -> 2694 (+12)  the cassette input is
#     fenced on a //c the way $C020 already was ($C060 is the 40/80-column
#     switch there and used to start the transport).
#   src/hgrpaint/HgrPaintEditor.cpp 2653 -> 2656 (+3) the clip records the
#     block geometry it was copied with (bug hunt #17).
# 2026-09-10 (native //c IOU mouse, 93d3d28): one ceiling raised, after the
# fact — the ratchet reported it and the commit did not raise it. The //c
# mouse is part of the motherboard, so Memory owns two seams of it: the IOU
# soft switches ($C015/$C017 status aliases, $C040-$C06F) routed to the
# slot-4 IIcMouse before the //e decode, and its state carried in the file
# snapshot as a length-prefixed section. The snapshot trailer is still the
# split owed on this file (see bug hunt #13).
#   src/Memory.cpp               2694 -> 2713 (+19)
# 2026-09-11 (chains of eight + the HDV card's drive 2): one ceiling raised
# for the wiring only. The feature code lives in its own TU
# (StorageCoordinator_Chains.cpp: the Liron's unit count command and restore,
# drive 2's settings / rebuild-snapshot / restore paths); what remains here is
# one call per historical site and the bay-aware `hdv_path` branch. +4 of it
# predates this change (the 2026-09-10 background persistence left the file
# at 2361 against 2357, which the ratchet reported and nobody raised).
#   src/StorageCoordinator.cpp   2357 -> 2372 (+15)
src/Apple2Display.cpp                    2522
src/DiskImage.cpp                        3154
src/ImageWriter.cpp                      2798
src/M6502.cpp                            2346
src/Memory.cpp                           2713
src/StorageCoordinator.cpp               2372
src/hgrpaint/HgrPaintEditor.cpp          2656
